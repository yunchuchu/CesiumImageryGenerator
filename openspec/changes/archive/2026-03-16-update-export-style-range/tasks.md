## 1. 导出服务参数与样式输入支持

- [x] 1.1 在导出服务中规范 `POST /api/exports` 请求体结构，确保包含 `style`（样式 JSON）与 `export`（导出参数）两个顶层字段。
- [x] 1.2 在服务端实现中支持从请求体接收 Web 端导出的样式 JSON，并替换当前仅使用默认样式的逻辑。
- [x] 1.3 在导出参数结构中增加或规范 `minZoom` / `maxZoom` 字段，未显式传入时在服务端统一回退到 `0` 与 `17`。
- [x] 1.4 在导出实现中根据 `minZoom` / `maxZoom` 控制实际导出缩放级别区间，仅渲染闭区间内的瓦片。

## 2. 导出脚本与指令优化

- [x] 2.1 重构或新增导出示例脚本（参考 `service/scripts/export-demo.sh`），支持通过参数指定样式文件路径与缩放级别区间。
- [x] 2.2 在脚本中实现默认行为：无参数时使用共享默认样式并导出 0–17 级；提供参数时使用外部样式与自定义缩放级别范围。
- [x] 2.3 在脚本中对请求体进行构造与打印，便于开发者理解可直接用 `curl`/HTTP 客户端复用的请求格式。

## 3. 文档与开发者指令补充

- [x] 3.1 更新 `VectorTileToCesiumImageryLayerTiles` 的 README，说明如何从 Web 端导出样式并传入导出服务。
- [x] 3.2 在 README 中新增导出缩放级别说明与推荐实践（例如默认 0–17、如何配置 7–14 等）。
- [x] 3.3 在 README 或单独文档中增加“指令版本”的使用示例，给出完整的一条或多条推荐命令行（包括样式路径、缩放级别与导出范围），方便开发者直接复制使用。

